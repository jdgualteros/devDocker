pipeline {
    agent {
        docker {
            image 'maven'
            args '-v /mnt/c/Users/LENOVO/.m2:/root/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'ls -l /root/.m2'
                sh 'cd /var/jenkins_home/workspace/devDocker_main/app && mvn -B -DskipTest clean package'
            }
        }
    }
}
