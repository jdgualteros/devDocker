pipeline {
    agent {
        docker {
            image 'maven:1.0'
            args '-v $HOME/.m2:/root/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTest clean package -s settings-nexus.xml'
            }
        }
    }
}
